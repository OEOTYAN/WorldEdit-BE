{
    "name": "${pluginName}",
    "entry": "${pluginFile}",
    "version": "${pluginVersion}",
    "type": "native",
    "dependencies": [
        {
            "name": "BedrockServerClientInterface",
            "version": "0.1.3"
        }
    ]
}